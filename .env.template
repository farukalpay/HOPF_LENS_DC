# Environment variables for HOPF_LENS_DC benchmarks
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# OpenAI API key for LLM calls
OPENAI_API_KEY=your_api_key_here

# Optional: Model override (default: gpt-4o)
# BENCHMARK_MODEL=gpt-4o

# Optional: Temperature override (default: 0.2)
# BENCHMARK_TEMPERATURE=0.2

# Optional: Max tool calls override (default: 6)
# BENCHMARK_MAX_TOOL_CALLS=6

# Optional: Timeout override in seconds (default: 30)
# BENCHMARK_TIMEOUT=30

# Optional: Random seed (default: 42)
# BENCHMARK_SEED=42
